fastapi==0.115.6
# main.py imports ProxyHeadersMiddleware from Uvicorn at application startup.
# The base package is sufficient on Vercel; the local-only [standard] extras
# remain in requirements-dev.txt.
uvicorn==0.34.0
supabase==2.7.0
python-dotenv==1.0.1
joblib==1.4.2
# The persisted scaler was produced with NumPy 2 (it references
# ``numpy._core.multiarray``).  NumPy 1.x cannot unpickle it, which makes the
# FastAPI lifespan fail on Vercel before the first request can be served.
numpy==2.1.3
pandas==2.2.3
shapely==2.0.6
# The trained model is CPU-inferred. The standard Linux xgboost package ships
# CUDA support and exceeds Vercel's function bundle limit; this package still
# exposes the `xgboost` Python module but omits unused GPU algorithms.
xgboost-cpu==2.1.4
pydantic==2.10.4
pydantic-settings==2.6.1
python-multipart==0.0.31
openai>=1.30.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1
email-validator>=2.0.0
httpx==0.27.0
# Match the version that serialized ``sekanet_scaler_shp.pkl``.
scikit-learn==1.6.1
slowapi==0.1.9
sentry-sdk[fastapi]==2.19.2
prometheus-fastapi-instrumentator==7.0.0
sendgrid==6.11.0
